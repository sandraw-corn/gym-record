# =============================================================================
# Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values.
# The .env file is gitignored and should contain your real API keys.

# =============================================================================
# Google Gemini API
# =============================================================================
# Get your API key at: https://aistudio.google.com/apikey
# Used for LLM-based workout log formatting (Chinese → English CSV)
GOOGLE_API_KEY=your_gemini_api_key_here

# =============================================================================
# Gemini Model Configuration
# =============================================================================
# Model to use for text processing (recommended: gemini-2.5-flash for speed/cost)
GEMINI_MODEL=gemini-2.5-flash

# Temperature for generation (0.0 = deterministic, 1.0 = creative)
# For structured data extraction, use low temperature (0.1-0.2)
GEMINI_TEMPERATURE=0.1

# Maximum output tokens (8192 max for Flash, use high limit to avoid truncation)
# Long workout sessions with many exercises can generate 3000-5000 tokens
GEMINI_MAX_OUTPUT_TOKENS=8192

# =============================================================================
# NOTES
# =============================================================================
# 1. Never commit .env to git - it contains your real API key
# 2. Get free Gemini API key at: https://aistudio.google.com/apikey
# 3. The formatter uses Gemini Flash 2.5 for fast, cheap Chinese log parsing
# 4. Low temperature (0.1) ensures consistent structured output
